using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InvIcon : MonoBehaviour
{

    void Start()
    {
    }

    void OnMouseDown()
    {
        Debug.Log("Rolou o click.");

    }
    // Start is called before the first frame update

    // Update is called once per frame
    void Update()
    {
        
    }
}
